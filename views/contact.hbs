<!DOCTYPE html>
<html>
<head>
    <title>{{title}}</title>
    <meta charset="utf-8" />
</head>
<body>
    {{> header}}
    <h1>Контакты</h1>
    <p>Электронный адрес: admin@mycorp.com</p>
    {{#if showList}}
        <ol>
            {{#each list}}
                <li>{{this}}</li>
            {{/each}}
        </ol>
    {{/if}}
</body>
<html>